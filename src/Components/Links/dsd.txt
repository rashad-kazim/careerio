// Replace the second and third 4 digits with *
      let arr = formattedInput.split("");
      arr.forEach((elem, ind) => {
        if (ind > 4 && ind < 14 && elem.trim() !== "") {
          arr[ind] = "*";
        }
      });

      const maskedSpanValue = arr.join("");

      spanMask.forEach((elem, ind) => {
            if (ind > 4 && ind < 14 && elem.trim() !== "") {
              spanMask[ind] = "*";
            }
          });